import { css } from 'lit';

export const styles = css`
  :host {
    display: inline;
    background: var(--grad);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }
`;
